// window.addEventListener('load', function() {
//     var img = document.querySelector('.desk-image'); // 이미지 선택
//     var container = document.querySelector('.desk-container'); // desk-container 선택
//     var overlay = document.querySelector('.desk-overlay'); // desk-overlay 선택
//
//     // 이미지의 실제 크기를 기반으로 desk-container와 desk-overlay의 크기 조정
//     container.style.width = img.offsetWidth + 'px';
//     container.style.height = img.offsetHeight + 'px';
//     overlay.style.width = img.offsetWidth + 'px';
//     overlay.style.height = img.offsetHeight + 'px';
// });
